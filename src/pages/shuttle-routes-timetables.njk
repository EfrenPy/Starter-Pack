---js
{
  pagination: { data: "site.languages", size: 1, alias: "langObj" },
  eleventyComputed: {
    lang: (data) => data.langObj.code,
    locale: (data) => ({ es: "es_ES", en: "en_US", it: "it_IT", fr: "fr_FR" }[data.langObj.code]),
    permalink: (data) => `${data.langObj.code}/shuttle-routes-timetables/`,
    title: (data) => data.pages['intent-landings']['shuttle-routes-timetables'].title[data.langObj.code],
    description: (data) => data.pages['intent-landings']['shuttle-routes-timetables'].description[data.langObj.code],
    og: (data) => ({
      title: data.pages['intent-landings']['shuttle-routes-timetables'].title[data.langObj.code],
      description: data.pages['intent-landings']['shuttle-routes-timetables'].description[data.langObj.code],
    }),
    breadcrumbs: (data) => {
      const lang = data.langObj.code;
      return [
        { label: data.ui[lang].nav_home, url: `/${lang}/` },
        { label: data.pages['intent-landings']['shuttle-routes-timetables'].title[lang] }
      ];
    }
  },
  layout: "layouts/page.njk"
}
---
{% set t = pages['intent-landings']['shuttle-routes-timetables'] %}
{% set l = lang %}

<h1>{{ t.title[l] }}</h1>

<div class="quick-answer">
  <strong>{% if l == 'es' %}Respuesta rápida:{% elif l == 'it' %}Risposta rapida:{% elif l == 'fr' %}Réponse rapide:{% else %}Quick answer:{% endif %}</strong>
  {{ t.description[l] }}
</div>

<h2>{% if l == 'es' %}Pasos clave{% elif l == 'it' %}Passi chiave{% elif l == 'fr' %}Étapes clés{% else %}Key steps{% endif %}</h2>

<ol>
  {% for step in t.steps[l] %}
  <li>{{ step }}</li>
  {% endfor %}
</ol>

<h2>{% if l == 'es' %}También te puede servir{% elif l == 'it' %}Ti può servire anche{% elif l == 'fr' %}Peut aussi vous aider{% else %}You may also need{% endif %}</h2>

<ul>
  <li><a href="/{{ l }}/cern-shuttles/">{% if l == 'es' %}Servicio de Lanzaderas CERN{% elif l == 'it' %}Servizio Navette CERN{% elif l == 'fr' %}Service Navettes CERN{% else %}CERN Shuttle Service{% endif %}</a></li>
  <li><a href="/{{ l }}/transportation/">{% if l == 'es' %}Transporte cerca del CERN{% elif l == 'it' %}Trasporti vicino al CERN{% elif l == 'fr' %}Transports près du CERN{% else %}Transportation near CERN{% endif %}</a></li>
</ul>
